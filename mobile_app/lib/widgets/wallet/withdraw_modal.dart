import 'package:flutter/material.dart';

class WithdrawModal extends StatelessWidget {
  const WithdrawModal({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("WithdrawModal")),
      body: const Center(child: Text("WithdrawModal Screen")),
    );
  }
}
